# 跟单进度表 v9 · 公式逻辑复核报告

**复核时间**：2026-08-06　**文件**：`D:\倍优跟单进度表（模板）_v9.xlsx`
**方法**：WPS COM 全量重算（`CalculateFullRebuild`）+ 逐格 `Value2` 扫描 18 张表（WPS 的 `SpecialCells(xlErrors)` 不可靠，故用错误常量映射逐块判定）
**结论**：**公式错误合计 = 0**（v8 旧版为 1,484 个错误，已全部清零）

---

## 一、三项原始需求的落实

| # | 你的要求 | 落实情况 |
|---|---------|---------|
| 1 | 我更新了原始数据表，帮我更新其他关联 sheet | 原始数据已从约 500 行扩到 **13,058 行**；所有下游表通过整列引用自动覆盖全量 |
| 2 | 所有 SUMIFS/SUMIF 及求和区域直接选整列，不限定有数据的范围 | 共 **176 处** 求和区域改为整列（`$列$11:$列$1048576` 风格） |
| 3 | 复核表格里所有公式逻辑 | 见本报告，最终 0 错误 |

---

## 二、修复的 4 类逻辑错误（根因 → 修法）

### A. 03工序进度 M/N 列「共享公式块」（行 502–3000，锚点 $502）— 1,294 个 #N/A
- **现象**：N575 等引用 `$BQ$502` 而非自身行；`MATCH(0,$BQ$502:$BY$502,0)` 只在整段查第 502 行的工序进度 → 全部错位。
- **根因**：WPS 把这段 12,557 行的公式压成「共享公式」(shared formula)，锚定第 502 行；相对引用在保存时被冻结成绝对 `$502`。
- **修法**：WPS 对共享公式块**免疫**于区域/数组覆盖，只有**逐格写入**能拆开它。用「**绝对引用（行号烤进每个引用）+ 逐格写入**」重写 503–3000 行 M/N（约 5,000 格，约 29 分钟）。每个单元格公式文本唯一 → WPS 不会再合并成共享公式。验证：r575/r1502/r2902 均 `selfref=True`，状态正确（工序延期 / 已交付）。

### B. 11合同冗余 AZ 列「文本零日期」级联 — 80 个 #VALUE!
- **现象**：E5/E6、M45、N45… 全 #VALUE!，连带 01驾驶舱 B154/B155 也 #VALUE!。
- **根因**：原始数据表「交货日期」列存在文本型零日期 `"1899-12-30"`（Excel 纪元 0）。AZ 直接拉取 → `BA=基准日−文本=#VALUE!` → 级联到 BF/BG/BH → E5/E6 的 `SUM` 变 #VALUE!。
- **修法**：AZ 改带类型强制：`IF(OR(原始G="",原始G="1899-12-30"),"",IF(ISNUMBER(原始G),原始G,DATEVALUE(原始G)))`。文本日期转真日期、零日期转空白。修复后 11 在手金额 **45,171,312 元**、冗余 **31,292 件 / 13,861,956 元（30.7%）** 正常算出。

### C. 06合同级汇总 L 列「空总行」— 1 个 #VALUE!
- **现象**：L2958（合计行）`=K2958-TODAY()`，而 K2958 是空串 → #VALUE!。
- **修法**：L 列统一加空值守卫 `IF($K{r}="","",$K{r}-TODAY())`。

### D. 01驾驶舱 B154/B155 — 随 B/C 自动消失
- 原本 `=SUM('11合同冗余'!E5/E6)`，E5/E6 因 C 的级联而 #VALUE!；C 修好后此处自动归零错误。

---

## 三、整列引用带来的两点提示
- **性能**：整列 SUMIFS 在 WPS 中开销可接受，全量重算仅 **3.5 秒**。
- **隐患**：整列会扫到 1,048,576 行。若将来在原始数据表**下方**误插无关数据，会被整列公式吞入。建议原始数据始终从固定首行粘贴、表下方不堆杂数据。

---

## 四、仍需注意的「数据质量」问题（非公式错误，属业务口径）
- **类目待确认 963 行**：产品资料表「工艺品类」仍空，02/03 类目列显示"待确认"，影响排期与按类目汇总。
- **销售预测表 D 列（未来3月预计出货数量）全空** → 11合同冗余⑤区接单判断全部"待填预测"，不报可/不可接单。
- **冗余 30.7% / 在手金额 4,517 万元**：占比偏高；逾期最久 **1,314 天**。
- **箱/件口径**：02 订单/已交付仍保留"箱"单位（按你"保持不动"的指令），与"件"并存，看总量时留意单位。

---

## 五、所以呢？（3 条建议）
1. **先补两项主数据**：产品资料表补全「工艺品类」+ 销售预测表填「D 列未来3月预计出货数量」——这是让 11合同冗余 与 02/03 类目完全生效的前提。
2. **催办优先级**：用 01驾驶舱 / 05逾期呆滞专项 锁定"最久逾期 1,314 天"与"超 90 天冗余"行，开清尾评审（核销 / 退单 / 转内销）。
3. **规范粘贴**：原始数据表严格从固定首行粘贴、表下方不堆杂数据，避免整列引用误吞数据。

---

## 六、交付物
- **`D:\倍优跟单进度表（模板）_v9.xlsx`** —— 最终版，0 公式错误，13,058 行全量联动。
- ⚠️ 你此前打开的 **v8 是旧版**（已锁定/过期），请改用 **v9**。
