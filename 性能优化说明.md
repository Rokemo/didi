# 跟单进度表性能优化说明（v9 → v10）

> 优化对象：`D:\倍优跟单进度表（模板）_v9.xlsx`
> 交付文件：`D:\倍优跟单进度表（模板）_v10.xlsx`
> 优化日期：2026-08-07｜复核方式：WPS 全量重算 + 逐格错误扫描

## 一、卡顿根因（量化）

用 WPS COM 读数组对 v9 做公式画像，定位到 3 类性能债：

| 根因 | v9 数量 | 影响 |
|---|---|---|
| 整列引用（如 `SUMIFS(A:A, …)`） | **329,334 处** | 每个 SUMIFS 每次重算扫描 1,048,576 行 → 105,849 个 SUMIFS 合计扫描约千亿单元格 |
| 显式全表引用（`…$1048576`） | 7 处 | 同上，整列到末行 |
| `TODAY()` 易失函数 | **42,132 处** | 每次重算（含打开）全部 4.2 万处易失函数重算 |

这三者叠加，使打开/交互时全量重算极慢，即"打开后明显卡顿"。

## 二、优化手段（不影响功能）

按你选择的「**保留自动更新**」路线：

1. **整列引用收口到上限行**：所有 `X:X` / `$X:$X` / 多列 `X:Y` 改为 `X2:X100000`（扫描从 104 万行降到 10 万行，约 **10 倍**）。
   - 仍保留"粘贴新行自动延展"能力（上限 10 万行 ≈ 当前 1.3 万行的 7.6 倍余量）。
2. **`TODAY()` 收口到单一锚点**：4.2 万处 `TODAY()` 统一改为引用 `00使用说明!C2`（该格 `=TODAY()`），全表仅剩 **1 处**易失函数。
3. **7 处 `1048576` 显式全表引用**同步收口到 100000。

### 关键技术点
- 大表（02/11/06/01）的整列引用是 **WPS 共享公式块**，逐格写太慢（33 万处 × 225ms 不可行）、整片数组赋值改不动。
- 最终用 **`Range.Replace` 按"精确整列令牌"整片替换**（如把令牌 `AC:AC` 一次性替换为 `AC2:AC50000`），WPS 内部单操作完成，对共享块有效、零错位、速度快（全本 16 表约 6 分钟）。

## 三、优化前后对比

| 指标 | v9 | v10 |
|---|---|---|
| 整列引用 | 329,334 | **0** |
| 显式 `1048576` | 7 | **0** |
| `TODAY()` 易失函数 | 42,132 | **1**（仅锚点） |
| 公式错误（16 表） | 0 | **0** |
| 全量重算耗时 | 卡顿明显 | 约 21 秒（最慢的"全量重建"模式；正常打开更快） |
| 关键 KPI | 完成率 83.9% / 在手 4,517 万 / 冗余 31,292 件·30.7% | **完全一致** |

KPI 抽样（v10 复核）：订单 2,063,476 / 已交付 1,731,046 / 完成率 83.9% / 逾期 2,749 行 / 11 在手金额 45,171,312 / 冗余 31,292 件·13,861,956·30.7% / 10 满足率 75.0%。所有数值与 v9 一致，确认优化未改变任何业务结果。

## 四、使用注意

1. **当前打开的是 v9（旧版）**，请改用 **v10**。
2. 若原始数据**超过 10 万行**，需把各表求和上限 `100000` 同步调大（否则超出行数不计入）。
3. `00使用说明!C2`（今日日期锚点）**请勿删除**，否则 4.2 万处引用会报错。
4. 文件末尾（00使用说明 第 80 行起）已写入本优化说明。

## 五、进一步的优化空间（可选）

若打开仍偏慢，剩余成本来自 03/04 两张表共约 **128 万公式格**（工序进度标志 + 状态列，均为逐行绝对引用，已无法再压缩扫描范围）。可选方案：
- 将 03/04 中"不随日常变动"的计算列转为值（牺牲部分实时性换速度）；
- 或整体切到**手动计算模式**（打开秒开，按 F9 刷新）——但此方案会关闭自动更新，与本次"保自动更新"的选择相悖，故未采用。
