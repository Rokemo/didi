# 跟单进度跟踪表 v3（二次改版）— 交付概述

## 本次新增/变更
1. **产品资料表（新·你维护）**：导入 D:\产品资料模板.xlsx（1675 行，产品编号/中文品名/工艺品类）。
   - 02 标准工艺类目改为 `VLOOKUP(产品编号 → 工艺品类)`，**不再由系统推断**。
   - 工艺品类留空/产品编号不在表 → 02/03 显示「待确认」（黄）。
2. **发货需求判断模块 `10发货需求判断`（新）**：
   - 新增 `发货需求表`（导入 D:\发货需求模板.xlsx，111 行：SKU/团队/运营/工厂/跟单/发货数量/发货时间）。
   - 设「发货开始日 / 结束日」（留空=不限制）即看**窗口内**需求。
   - 需求 = 发货需求表窗口内「发货数量」按 **产品编号+团队** 汇总；
   - 供应 = 原始数据表「未入库数量」按 **产品编号+团队** 汇总（按你要求用未入库数量）；
   - 差额 ≥ 0 → 满足✔，否则 → 缺口 N。驾驶舱⑥ 引用其汇总（需求/未入库/缺口/缺口行/满足率）。
3. **类目推断彻底下线**：原报关材质→类目推断移除；08字典①区改为标准工艺类目总表参考。

## 验证结果
- WPS COM 全量重算：**TOTAL FORMULA ERRORS: 0** ✅
- 发货需求判断实测 KPI：窗口内需求 **2525** / 可供应未入库 **3350** / 总缺口 **825** / 缺口行 **76** / 满足率 **29.6%**。
- VLOOKUP 链实测：在 产品资料表 填某产品编号的工艺品类 → 02 类目即时变更；还原 → 回到「待确认」。

## 重要提醒
- **产品资料表「工艺品类」当前全空**（你说了先空着、你后续手填）。填之前 02/03 整列显示「待确认」、排期出不来——这是预期，填完即自动恢复正常。
- 填 工艺品类 时请用标准口径（编藤 / 铁艺铝艺-普通 / 特斯林 / 树脂…），否则 07 节拍库匹配不到仍会待确认。该列已挂下拉。
- **发货需求判断的「团队」须与原始数据表团队写法完全一致**（如 `鄢紫薇/UP-US`），否则会误报缺口。
- 交付文件改名 `理单-跟单进度跟踪表_v3.xlsx`（原 v2 文件正被 WPS 占用，避免覆盖锁定的旧文件）。

## 文件
- `理单-跟单进度跟踪表_v3.xlsx`（14 张表）
